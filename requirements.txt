# Stocker — dependencias mínimas del MVP
# (descarga, almacenamiento y modelo; ver CONTEXTO.md para el diseño completo)

yfinance
pandas
sqlalchemy
scikit-learn
pandas_market_calendars
joblib
numpy
requests
python-dotenv
# >=1.50: temas [theme.light]/[theme.dark], fuentes por URL y st.context.theme (rediseño 2026-09-17)
streamlit>=1.50
plotly
streamlit-echarts
anthropic

# Opcional — solo si se usa `python model.py --model lightgbm` (con o sin
# --tune). El resto del pipeline (random_forest, logistic) no depende de
# esto — import perezoso dentro de model.py, ver CONTEXTO.md "Plan B:
# Gradient Boosting".
lightgbm

# Opcional — solo si se activa el fallback de descarga en
# src/fallback/yahooquery_download.py (yfinance sigue siendo la fuente
# principal, ver CONTEXTO.md). No se instala por defecto:
# yahooquery
